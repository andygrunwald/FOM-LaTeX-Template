dist: trusty
before_install:
  - sudo add-apt-repository ppa:jonathonf/texlive-staging -y
  - sudo apt-get update

install:
  - sudo apt-get install -y

script:
  - pdflatex --version
  - biber --version
  - ./compile.sh
